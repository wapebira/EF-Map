EF-Map – Grafana assets

- world_api_dlt_dashboard.json: Import this into Grafana (Dashboards → Import). Before importing, replace POSTGRES_DS_UID with your Postgres datasource UID or select the datasource after import.
- Assumes tables exist in schema world_api_dlt (created by the ingestion scaffold).
