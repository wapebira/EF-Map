Local Snapshot & Inventory

Scripts:

# [DEPRECATED] Local Decode Pipeline (Typed Rows)

Deprecated in favor of using Primordium pg-indexer + downstream materialization in Postgres. See `docs/DEPRECATIONS.md`.
- npm run local:snapshot
Env:
- LOCAL_DB_PATH, STATUS_PATH, CONTROL_PATH to override defaults.
Env:
