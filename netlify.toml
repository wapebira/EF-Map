#[ Netlify root configuration ]
# Ensures builds run from the front-end app subdirectory and functions are detected.

[build]
  base = "eve-frontier-map"
  publish = "eve-frontier-map/dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "eve-frontier-map/netlify/functions" # correct path is actually eve-frontier-map/netlify/functions but folder is misspelled 'netlify'
  node_bundler = "esbuild"
